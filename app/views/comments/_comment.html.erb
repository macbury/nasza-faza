<li id="comment_<%= comment.id %>">
	<p>
		<%= comment.body %>
		<br /><small>
			dodano <%= distance_of_time_in_words_to_now comment.created_at, true %> temu przez <b><%= comment.user.login %></b>
		</small>
	</p>
</li>